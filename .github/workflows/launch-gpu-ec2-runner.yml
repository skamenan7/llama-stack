name: 'Launch GPU EC2 Runner'

on:
  push:
    branches:
      - 'feat/vLLM-GPU-runners'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Dummy step
        run: echo "Launch GPU EC2 Runner placeholder"
